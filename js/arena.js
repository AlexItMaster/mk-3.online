/**
 * Ф-я добавления нового елемента на арену
 *
 * @param {object} elem - Елемент который нужно добавить на арену
 */
const addingToArena = (elem) => {
    $ARENAS.appendChild(elem);
}

export default addingToArena;